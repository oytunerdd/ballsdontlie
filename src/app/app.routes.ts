import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
    { path: 'tr-TR', component: AppComponent },
    { path: 'en-US', component: AppComponent },


];

